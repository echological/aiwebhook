# AI Webhook - Application Config
# Base config (shared all profiles)

quarkus:
  http:
    port: 8080
  dev-ui:
    always-include: true
  log:
    level: INFO
    category:
      "org.techsolution": DEBUG
  smallrye-openapi:
    path: /q/openapi
    ui:
      path: /q/swagger-ui
app:
  name: AI Webhook
  version: 1.0.0
  env: dev
